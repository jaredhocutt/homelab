---

ansible-navigator:
  logging:
    level: debug
    file: .ansible/logs/ansible-navigator.log

  playbook-artifact:
    enable: true
    replay: .ansible/playbook-artifacts/{playbook_name}-artifact-{time_stamp}.json
    save-as: .ansible/playbook-artifacts/{playbook_name}-artifact-{time_stamp}.json

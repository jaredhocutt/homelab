---

common_traefik_domain: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.apps_common_traefik_domain) }}"

shared_volume_download_books: download-books

###############################################################################
# authentik
###############################################################################

authentik_image_tag: "2026.2.1"  # skopeo list-tags docker://ghcr.io/goauthentik/server | jq -r '.Tags[] | select(test("^[0-9\\.]+$"))'
authentik_postgres_image_tag: "16.13"  # skopeo list-tags docker://docker.io/library/postgres | jq -r '.Tags[] | select(test("^16\\.[0-9]"))'

authentik_postgres_password: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.authentik_postgres_password) }}"
authentik_secret_key: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.authentik_secret_key) }}"

authentik_server_container_env:
  AUTHENTIK_EMAIL__HOST: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.authentik_email_host) }}"
  AUTHENTIK_EMAIL__PORT: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.authentik_email_port) }}"
  AUTHENTIK_EMAIL__USERNAME: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.authentik_email_username) }}"
  AUTHENTIK_EMAIL__FROM: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.authentik_email_from) }}"
  AUTHENTIK_EMAIL__USE_SSL: "true"
authentik_worker_container_env: "{{ authentik_server_container_env }}"

authentik_secrets:
  authentik_email_password: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.authentik_email_password) }}"
authentik_server_container_secrets:
  - authentik_email_password,type=env,target=AUTHENTIK_EMAIL__PASSWORD
authentik_worker_container_secrets: "{{ authentik_server_container_secrets }}"

newt_authentik_secrets:
  newt_authentik_newt_id: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.newt_authentik_newt_id) }}"
  newt_authentik_newt_secret: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.newt_authentik_newt_secret) }}"
newt_authentik_container_secrets:
  - newt_authentik_newt_id,type=env,target=NEWT_ID
  - newt_authentik_newt_secret,type=env,target=NEWT_SECRET


###############################################################################
# audiobookshelf
###############################################################################

audiobookshelf_image_tag: "2.33.1"  # skopeo list-tags docker://ghcr.io/advplyr/audiobookshelf | jq -r '.Tags[] | select(test("^[0-9]"))'

newt_audiobookshelf_secrets:
  newt_audiobookshelf_newt_id: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.newt_audiobookshelf_newt_id) }}"
  newt_audiobookshelf_newt_secret: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.newt_audiobookshelf_newt_secret) }}"
newt_audiobookshelf_container_secrets:
  - newt_audiobookshelf_newt_id,type=env,target=NEWT_ID
  - newt_audiobookshelf_newt_secret,type=env,target=NEWT_SECRET

###############################################################################
# backrest
###############################################################################

backrest_version: v1.11.2

backrest_traefik_host: "backrest-apps.{{ common_traefik_domain }}"

backrest_container_env:
  BACKREST_PORT: 0.0.0.0:9898

###############################################################################
# bar_assistant
###############################################################################

bar_assistant_image_tag: "5.13.2"  # skopeo list-tags docker://docker.io/barassistant/server | jq -r '.Tags[] | select(test("^5\\.[0-9]"))'
bar_assistant_salt_rim_image_tag: "4.14.1"  # skopeo list-tags docker://docker.io/barassistant/salt-rim | jq -r '.Tags[] | select(test("^4\\.[0-9]"))'
bar_assistant_redis_image_tag: "8.6.2"  # skopeo list-tags docker://docker.io/library/redis | jq -r '.Tags[] | select(test("^[0-9]\\.[0-9\\.]*$"))'
bar_assistant_meilisearch_image_tag: v1.15.2  # skopeo list-tags docker://docker.io/getmeili/meilisearch | jq -r '.Tags[] | select(test("^v1.15"))'

bar_assistant_meilisearch_master_key: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.bar_assistant_meilisearch_master_key) }}"

###############################################################################
# bentopdf
###############################################################################

bentopdf_image_tag: v2.7.0  # skopeo list-tags docker://ghcr.io/alam00000/bentopdf-simple | jq -r '.Tags[] | select(test("^v[0-9\\.]+$"))'

###############################################################################
# calibre_web_automated
###############################################################################

calibre_web_automated_image_tag: v4.0.6  # skopeo list-tags docker://ghcr.io/crocodilestick/calibre-web-automated | jq -r '.Tags[] | select(test("^v[0-9]"))'

calibre_web_automated_volume_ingest: "{{ shared_volume_download_books }}"

calibre_web_automated_container_env:
  PUID: "{{ global_media_uid }}"
  PGID: "{{ global_media_gid }}"

calibre_web_automated_secrets:
  calibre_web_automated_hardcover_token: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.calibre_web_automated_hardcover_token) }}"
calibre_web_automated_container_secrets:
  - calibre_web_automated_hardcover_token,type=env,target=HARDCOVER_TOKEN

###############################################################################
# changedetection
###############################################################################

changedetection_image_tag: "0.54.7"  # skopeo list-tags docker://ghcr.io/dgtlmoon/changedetection.io | jq -r '.Tags[] | select(test("^[0-9]"))'
changedetection_browser_image_tag: "0.0.3"  # skopeo list-tags docker://docker.io/dgtlmoon/sockpuppetbrowser | jq -r '.Tags[] | select(test("^[0-9]"))'

###############################################################################
# go2rtc
###############################################################################

go2rtc_image_tag: "1.9.14"  # skopeo list-tags docker://ghcr.io/alexxit/go2rtc | jq -r '.Tags[] | select(test("^[0-9\\.]+$"))'

###############################################################################
# healthchecks
###############################################################################

healthchecks_image_tag: version-v4.1.1  # skopeo list-tags docker://lscr.io/linuxserver/healthchecks | jq -r '.Tags[] | select(test("^version-v[0-9]"))'

healthchecks_email: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.healthchecks_email) }}"
healthchecks_password: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.healthchecks_password) }}"
healthchecks_secret_key: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.healthchecks_secret_key) }}"
healthchecks_container_env:
  DEFAULT_FROM_EMAIL: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.healthchecks_from_email) }}"
  EMAIL_HOST: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.healthchecks_email_host) }}"
  EMAIL_PORT: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.healthchecks_email_port) }}"
  EMAIL_HOST_USER: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.healthchecks_email_user) }}"
  EMAIL_USE_TLS: "True"
  REGISTRATION_OPEN: "False"

healthchecks_secrets:
  healthchecks_discord_client_id: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.healthchecks_discord_client_id) }}"
  healthchecks_discord_client_secret: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.healthchecks_discord_client_secret) }}"
  healthchecks_email_password: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.healthchecks_email_password) }}"
  healthchecks_pushover_api_token: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.healthchecks_pushover_api_token) }}"
  healthchecks_pushover_subscription_url: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.healthchecks_pushover_subscription_url) }}"
healthchecks_container_secrets:
  - healthchecks_discord_client_id,type=env,target=DISCORD_CLIENT_ID
  - healthchecks_discord_client_secret,type=env,target=DISCORD_CLIENT_SECRET
  - healthchecks_email_password,type=env,target=EMAIL_HOST_PASSWORD
  - healthchecks_pushover_api_token,type=env,target=PUSHOVER_API_TOKEN
  - healthchecks_pushover_subscription_url,type=env,target=PUSHOVER_SUBSCRIPTION_URL

###############################################################################
# homarr
###############################################################################

homarr_image_tag: v1.57.1  # skopeo list-tags docker://ghcr.io/homarr-labs/homarr | jq -r '.Tags[] | select(test("^v[0-9]"))'

homarr_api_key: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.homarr_api_key) }}"
homarr_secret_encryption_key: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.homarr_secret_encryption_key) }}"

###############################################################################
# karakeep
###############################################################################

karakeep_image_tag: "0.31.0"  # skopeo list-tags docker://ghcr.io/karakeep-app/karakeep | jq -r '.Tags[] | select(test("^[0-9\\.]+$"))'
karakeep_chrome_image_tag: "124"  # The project hard codes this tag, so let's keep it
karakeep_meilisearch_image_tag: v1.13.3  # The project hard codes this tag, so let's keep it

karakeep_nextauth_secret: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.karakeep_nextauth_secret) }}"
karakeep_meilisearch_master_key: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.karakeep_meilisearch_master_key) }}"


karakeep_secrets:
  karakeep_openai_api_key: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.karakeep_openai_api_key) }}"
karakeep_container_secrets:
  - karakeep_openai_api_key,type=env,target=OPENAI_API_KEY

###############################################################################
# newt
###############################################################################

newt_image_tag: "1.10.3"  # skopeo list-tags docker://docker.io/fosrl/newt | jq -r '.Tags[] | select(test("^[0-9]"))'

newt_endpoint: https://pangolin.{{ lookup('bitwarden.secrets.lookup', bws_uuid.pangolin_base_domain) }}

###############################################################################
# n8n
###############################################################################

n8n_image_tag: "2.14.2"  # skopeo list-tags docker://docker.n8n.io/n8nio/n8n | jq -r '.Tags[] | select(test("^[0-9]\\."))'
n8n_postgres_image_tag: "16.13"  # skopeo list-tags docker://docker.io/library/postgres | jq -r '.Tags[] | select(test("^16\\.[0-9]"))'

n8n_postgres_password: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.n8n_postgres_password) }}"

n8n_container_env:
  N8N_LOG_LEVEL: debug

###############################################################################
# nocodb
###############################################################################

nocodb_image_tag: "0.301.5"  # skopeo list-tags docker://docker.io/nocodb/nocodb | jq -r '.Tags[] | select(test("^[0-9]"))'
nocodb_postgres_image_tag: "16.13"  # skopeo list-tags docker://docker.io/library/postgres | jq -r '.Tags[] | select(test("^16\\.[0-9]"))'

nocodb_postgres_password: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.nocodb_postgres_password) }}"

###############################################################################
# open_webui
###############################################################################

open_webui_image_tag: v0.8.12  # skopeo list-tags docker://ghcr.io/open-webui/open-webui | jq -r '.Tags[] | select(test("^v[0-9\\.]+$"))'

###############################################################################
# palworld
###############################################################################

palworld_image_tag: v0.7.2.87654  # skypeo list-tags docker://ghcr.io/pocketpairjp/palserver | jq -r '.Tags[] | select(test("^v[0-9\\.]+$"))'

newt_palworld_secrets:
  newt_palworld_newt_id: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.newt_palworld_newt_id) }}"
  newt_palworld_newt_secret: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.newt_palworld_newt_secret) }}"
newt_palworld_container_secrets:
  - newt_palworld_newt_id,type=env,target=NEWT_ID
  - newt_palworld_newt_secret,type=env,target=NEWT_SECRET

###############################################################################
# prowlarr
###############################################################################

prowlarr_image_tag: version-2.3.0.5236  # skopeo list-tags docker://lscr.io/linuxserver/prowlarr | jq -r '.Tags[] | select(test("^version-[0-9]"))'

###############################################################################
# shelfmark
###############################################################################

shelfmark_image_tag: v1.2.1  # skopeo list-tags docker://ghcr.io/calibrain/shelfmark | jq -r '.Tags[] | select(test("^v[0-9]"))'

shelfmark_volume_books: "{{ shared_volume_download_books }}"

shelfmark_container_env:
  PUID: "{{ global_media_uid }}"
  PGID: "{{ global_media_gid }}"

###############################################################################
# snipe_it
###############################################################################

snipe_it_image_tag: v8.4.0  # skopeo list-tags docker://docker.io/snipe/snipe-it | jq -r '.Tags[] | select(test("^v[0-9]"))'

snipe_it_app_key: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.snipe_it_app_key) }}"
snipe_it_mariadb_root_password: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.snipe_it_mariadb_root_password) }}"
snipe_it_mariadb_password: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.snipe_it_mariadb_password) }}"

snipe_it_container_env:
  MAIL_HOST: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.snipe_it_mail_host) }}"
  MAIL_PORT: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.snipe_it_mail_port) }}"
  MAIL_USERNAME: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.snipe_it_mail_username) }}"
  MAIL_FROM_ADDR: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.snipe_it_mail_from) }}"
  MAIL_FROM_NAME: Snipe-IT
  MAIL_REPLYTO_ADDR: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.snipe_it_mail_from) }}"
  MAIL_REPLYTO_NAME: Snipe-IT
  MAIL_TLS_VERIFY_PEER: true
  MAIL_AUTO_EMBED_METHOD: attachment

snipe_it_secrets:
  snipe_it_mail_password: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.snipe_it_mail_password) }}"
snipe_it_container_secrets:
  - snipe_it_mail_password,type=env,target=MAIL_PASSWORD

###############################################################################
# stirling_pdf
###############################################################################

stirling_pdf_image_tag: 2.8.0-fat  # skopeo list-tags docker://docker.stirlingpdf.com/stirlingtools/stirling-pdf | jq -r '.Tags[] | select(test("^[0-9\\.]+-fat$"))'

stirling_pdf_container_env:
  DOCKER_ENABLE_SECURITY: "false"

###############################################################################
# tandoor
###############################################################################

tandoor_image_tag: "2.6.1"  # skopeo list-tags docker://docker.io/vabene1111/recipes | jq -r '.Tags[] | select(test("^[0-9]\\."))'
tandoor_postgres_image_tag: "16.13"  # skopeo list-tags docker://docker.io/library/postgres | jq -r '.Tags[] | select(test("^16\\.[0-9]"))'

tandoor_postgres_password: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.tandoor_postgres_password) }}"
tandoor_secret_key: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.tandoor_secret_key) }}"

###############################################################################
# traefik
###############################################################################

traefik_image_tag: v3.6.12  # skopeo list-tags docker://docker.io/library/traefik | jq -r '.Tags[] | select(test("^v[0-9\\.]+$"))'

traefik_traefik_host: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.traefik_traefik_host) }}"

traefik_command:
  - --certificatesresolvers.letsencrypt.acme.email={{ lookup('bitwarden.secrets.lookup', bws_uuid.traefik_letsencrypt_email) }}
  - --certificatesresolvers.letsencrypt.acme.storage=/certs/acme.json
  - --certificatesresolvers.letsencrypt.acme.dnschallenge.provider=cloudflare
  - --entrypoints.websecure.http.tls.certresolver=letsencrypt

traefik_secrets:
  traefik_letsencrypt_cloudflare_dns_api_key: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.traefik_letsencrypt_cloudflare_dns_api_key) }}"
  traefik_letsencrypt_cloudflare_zone_api_key: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.traefik_letsencrypt_cloudflare_zone_api_key) }}"
traefik_container_secrets:
  - traefik_letsencrypt_cloudflare_dns_api_key,type=env,target=CF_DNS_API_TOKEN
  - traefik_letsencrypt_cloudflare_zone_api_key,type=env,target=CF_ZONE_API_TOKEN

###############################################################################
# transfer_sh
###############################################################################

transfer_sh_image_tag: v1.6.1-noroot  # skopeo list-tags docker://docker.io/dutchcoders/transfer.sh | jq -r '.Tags[] | select(test("^v[0-9\\.]+\\-noroot$"))'

###############################################################################
# uptime_kuma
###############################################################################

uptime_kuma_image_tag: "2.2.1"  # skopeo list-tags docker://ghcr.io/louislam/uptime-kuma | jq -r '.Tags[] | select(test("^[0-9\\.]+$"))'

###############################################################################
# warracker
###############################################################################

warracker_image_tag: "1.0.2"  # skopeo list-tags docker://ghcr.io/sassanix/warracker/main | jq -r '.Tags[] | select(test("^[0-9]"))'
warracker_postgres_image_tag: "15.15"  # skopeo list-tags docker://docker.io/library/postgres | jq -r '.Tags[] | select(test("^15\\.[0-9]"))'

warracker_postgres_password: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.warracker_postgres_password) }}"
warracker_secret_key: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.warracker_secret_key) }}"

###############################################################################
# kickstart
###############################################################################

kickstart_hostname: apps
kickstart_network:
  - device: enp1s0f0
    activate: true
  - device: enp1s0f1
    activate: true
    options: --nodefroute
  - device: eno1
    activate: false
  - device: wlp0s20f3
    activate: false

kickstart_storage_disks:
  - name: disk/by-id/nvme-Samsung_SSD_960_EVO_250GB_S3ESNX0J407135J
    partitions:
      - mountpoint: /boot/efi
        filesystem: efi
        size: 600
        options: --fsoptions="umask=0077,shortname=winnt"
      - mountpoint: /boot
        filesystem: xfs
        size: 1024
      - mountpoint: pv.01
        filesystem: lvmpv
        size: 1
        options: --grow
  - name: disk/by-id/nvme-Samsung_SSD_980_PRO_2TB_S76ENU0WC12575X
    partitions:
      - mountpoint: pv.02
        filesystem: lvmpv
        size: 1
        options: --grow
kickstart_storage_volume_groups:
  - name: fedora
    physical_volumes:
      - pv.01
    logical_volumes:
      - name: root
        mountpoint: /
        filesystem: xfs
        size: "{{ 1024 * 50 }}"  # 50 GB
      - name: home
        mountpoint: /home
        filesystem: xfs
        size: "{{ 1024 * 30 }}"  # 30 GB
      - name: swap
        mountpoint: swap
        filesystem: swap
        size: "{{ 1024 * 4 }}"  # 4 GB
  - name: data
    physical_volumes:
      - pv.02
    logical_volumes:
      - name: podman
        mountpoint: /var/lib/containers
        filesystem: xfs
        percent: 100

kickstart_users:
  - fullname: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.kickstart_user_1_fullname) }}"
    username: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.kickstart_user_1_username) }}"
    groups: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.kickstart_user_1_groups) }}"
    uid: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.kickstart_user_1_uid) }}"
    gid: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.kickstart_user_1_gid) }}"
    password: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.kickstart_user_1_password_crypted) }}"
    authorized_keys:
      - "{{ lookup('bitwarden.secrets.lookup', bws_uuid.ssh_authorized_key_1) }}"
      - "{{ lookup('bitwarden.secrets.lookup', bws_uuid.ssh_authorized_key_2) }}"
      - "{{ lookup('bitwarden.secrets.lookup', bws_uuid.ssh_authorized_key_3) }}"
  - fullname: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.kickstart_user_2_fullname) }}"
    username: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.kickstart_user_2_username) }}"
    groups: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.kickstart_user_2_groups) }}"
    uid: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.kickstart_user_2_uid) }}"
    gid: "{{ lookup('bitwarden.secrets.lookup', bws_uuid.kickstart_user_2_gid) }}"
    sudoers: |
      {{ lookup('bitwarden.secrets.lookup', bws_uuid.kickstart_user_2_username) }} ALL=(ALL) NOPASSWD: ALL
      Defaults:{{ lookup('bitwarden.secrets.lookup', bws_uuid.kickstart_user_2_username) }} !requiretty
    authorized_keys:
      - "{{ lookup('bitwarden.secrets.lookup', bws_uuid.ssh_authorized_key_4) }}"

---
# vars file for jaredhocutt.homelab.radarr

radarr_env_vars_base:
  TZ: "America/New_York"

radarr_quadlet_options_base:
  - |
    [Service]
    Restart=always
  - |
    [Install]
    WantedBy=multi-user.target

---
# tasks file for jaredhocutt.homelab.blaze

- name: Pull container images
  containers.podman.podman_image:
    name: "{{ item.image }}:{{ item.image_tag }}"
    state: present
  become: true
  loop:
    - image: "{{ blaze_image }}"
      image_tag: "{{ blaze_image_tag }}"

- name: Create network
  containers.podman.podman_network:
    name: "{{ blaze_name }}"
    driver: bridge
    disable_dns: false
    opt:
      isolate: "true"
    quadlet_file_mode: "0600"
    state: quadlet
  become: true

- name: Create secrets
  containers.podman.podman_secret:
    name: "{{ item.key }}"
    data: "{{ item.value }}"
    state: present
  become: true
  no_log: true
  loop: "{{ blaze_secrets_combined | dict2items }}"

- name: Create blaze container
  containers.podman.podman_container:
    name: "{{ blaze_name }}"
    image: "{{ blaze_image }}:{{ blaze_image_tag }}"
    network:
      - "{{ blaze_name }}.network"
      - "{{ blaze_traefik_enable | ternary(common_traefik_network ~ '.network', omit) }}"
    env: "{{ lookup('jaredhocutt.homelab.container_config', 'env') }}"
    secrets: "{{ lookup('jaredhocutt.homelab.container_config', 'secrets') }}"
    publish: "{{ blaze_traefik_enable | ternary(omit, blaze_ports) }}"
    labels: "{{ lookup('jaredhocutt.homelab.container_config', 'labels') }}"
    quadlet_file_mode: "0600"
    quadlet_options: "{{ lookup('jaredhocutt.homelab.container_config', 'quadlet_options') }}"
    state: quadlet
  notify: Restart {{ blaze_name }}
  become: true

- name: Flush handlers
  ansible.builtin.meta: flush_handlers

---
# vars file for jaredhocutt.homelab.kometa

kometa_env_vars_base:
  TZ: "America/New_York"

kometa_quadlet_options_base:
  - |
    [Service]
    Restart=always
  - |
    [Install]
    WantedBy=multi-user.target

---
# tasks file for jaredhocutt.homelab.changedetection

- name: Pull container images
  containers.podman.podman_image:
    name: "{{ item.image }}:{{ item.image_tag }}"
    state: present
  become: true
  loop:
    - image: "{{ changedetection_image }}"
      image_tag: "{{ changedetection_image_tag }}"
    - image: "{{ changedetection_browser_image }}"
      image_tag: "{{ changedetection_browser_image_tag }}"

- name: Create network
  containers.podman.podman_network:
    name: "{{ changedetection_name }}"
    driver: bridge
    disable_dns: false
    opt:
      isolate: "true"
    quadlet_file_mode: "0600"
    state: quadlet
  become: true

- name: Create volumes
  containers.podman.podman_volume:
    name: "{{ item.name }}"
    quadlet_options: "{{ item.options | default(omit) }}"
    quadlet_file_mode: "0600"
    state: quadlet
  become: true
  loop:
    - name: "{{ changedetection_volume_data }}"
      options: "{{ changedetection_volume_data_options | default(omit) }}"

- name: Create secrets
  containers.podman.podman_secret:
    name: "{{ item.key }}"
    data: "{{ item.value }}"
    state: present
  become: true
  loop: "{{ changedetection_secrets_combined | dict2items }}"

- name: Create browser container
  containers.podman.podman_container:
    name: "{{ changedetection_name }}-browser"
    image: "{{ changedetection_browser_image }}:{{ changedetection_browser_image_tag }}"
    network: "{{ changedetection_name }}.network"
    env: "{{ changedetection_browser_container_env_combined }}"
    secrets: "{{ changedetection_browser_container_secrets_combined }}"
    cap_add:
      - SYS_ADMIN
    labels: "{{ changedetection_browser_container_labels_combined }}"
    quadlet_file_mode: "0600"
    quadlet_options: "{{ changedetection_browser_quadlet_options_combined }}"
    state: quadlet
  notify: Restart browser for {{ changedetection_name }}
  become: true

- name: Create changedetection container
  containers.podman.podman_container:
    name: "{{ changedetection_name }}"
    image: "{{ changedetection_image }}:{{ changedetection_image_tag }}"
    network:
      - "{{ changedetection_name }}.network"
      - "{{ changedetection_traefik_enable | ternary(common_traefik_network ~ '.network', omit) }}"
    env: "{{ changedetection_container_env_combined }}"
    secrets: "{{ changedetection_container_secrets_combined }}"
    volumes:
      - "{{ changedetection_volume_data }}.volume:/datastore"
    publish: "{{ changedetection_traefik_enable | ternary(omit, changedetection_ports) }}"
    labels: "{{ changedetection_container_labels_combined }}"
    quadlet_file_mode: "0600"
    quadlet_options: "{{ changedetection_quadlet_options_combined }}"
    state: quadlet
  notify: Restart {{ changedetection_name }}
  become: true

- name: Flush handlers
  ansible.builtin.meta: flush_handlers

---
# vars file for jaredhocutt.homelab.audiobookshelf

audiobookshelf_env_vars_base:
  TZ: "America/New_York"

audiobookshelf_quadlet_options_base:
  - |
    [Service]
    Restart=always
  - |
    [Install]
    WantedBy=multi-user.target

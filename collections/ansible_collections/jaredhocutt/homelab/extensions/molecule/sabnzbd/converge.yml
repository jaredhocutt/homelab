---
- name: Converge
  hosts: all
  gather_facts: false
  vars:
    sabnzbd_traefik_enable: false
  tasks:
    - name: Include sabnzbd role
      ansible.builtin.include_role:
        name: jaredhocutt.homelab.sabnzbd

---
- name: Converge
  hosts: all
  gather_facts: false
  vars:
    blaze_traefik_enable: false
  tasks:
    - name: Include blaze role
      ansible.builtin.include_role:
        name: jaredhocutt.homelab.blaze

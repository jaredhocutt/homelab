---
- name: Converge
  hosts: all
  gather_facts: false
  vars:
    immich_traefik_enable: false
    immich_postgres_password: test-postgres-password
  tasks:
    - name: Include immich role
      ansible.builtin.include_role:
        name: jaredhocutt.homelab.immich

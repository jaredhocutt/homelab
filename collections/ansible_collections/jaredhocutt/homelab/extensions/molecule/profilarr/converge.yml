---
- name: Converge
  hosts: all
  gather_facts: false
  vars:
    profilarr_traefik_enable: false
  tasks:
    - name: Include profilarr role
      ansible.builtin.include_role:
        name: jaredhocutt.homelab.profilarr
